#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <stdbool.h>
#include <string.h>



int findMax(int x, int y)
{
 
}

int main()
{
  char string1[] = "Eren";
  char string2[] = "Eren";
  
  //strlwr(string1);
  //strupr(string1);
  //strcat(string1,string2);
  //strncat(string1,string2, 1);   // if you want to take out any word you can use this  
  //strcpy(string1,string2);       // string2 copy to in string1 place 
  //strncpy(string1,string2,1);    // if you don't want all of the word is copy then you can use this funtion 
  //strset(string1, '?');            // if you use a pin code you can use this 
  //strnset(string1,'x',3);
  //strrev(string1); 

  // int length = strlen("string1");
  // int result = strcmp(string1, string2); // if they are same then return 0 if not then return 1
  // int result = strncmp(string1, string2, 1);
  // int result = strcmpi(string1, string2);    // ignora case sensitivity
  // int result = strnicmp(string1, string2, 1);

 // printf("%s", string1);
 // printf("%d", result);

  return 0;
}